# Chat Juicer Requirements

# Core AI Libraries (REQUIRED)
openai-agents>=0.1.0  # Provides agents module for Responses API support
openai>=1.0.0  # Azure OpenAI client library

# Configuration (REQUIRED)
python-dotenv>=1.0.0  # Load environment variables from .env file

# Document Conversion (REQUIRED for file reading features)
markitdown>=0.1.0  # Microsoft's markdown converter for PDF, Word, Excel, etc.

# Token Counting (REQUIRED)
tiktoken>=0.5.0  # OpenAI's official token counting library for accurate token counts

# Structured Logging (REQUIRED)
python-json-logger>=2.0.0  # JSON formatted logging for structured logs
